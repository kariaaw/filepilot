/**
 * Public exports for the shared IconButton component.
 *
 * Consumers depend on this public boundary instead of the component's
 * internal file structure.
 */
export {
  IconButton,
  type IconButtonProps,
  type IconButtonSize,
  type IconButtonVariant,
} from '@/shared/components/icon-button/icon-button';
