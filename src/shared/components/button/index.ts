/**
 * Public exports for the shared Button component.
 *
 * Consumers import from this directory rather than depending on the internal
 * file structure of the component.
 */
export {
  Button,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
} from '@/shared/components/button/button';
